# Greeting App

A modern greeting page with a centered UI and a random, non-overlapping background animation on each click.

## Run locally

From the `greeting-app` folder:

```powershell
python -m http.server 5173
```

Then open:

http://localhost:5173

